// Package version provides build version information.package version

package version

// Version is set at build time via ldflags.
var Version = "dev"

// BuildTime is set at build time via ldflags.
var BuildTime = "unknown"
